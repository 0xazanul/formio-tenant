name: Test Dependency Confusion
on: [push]

jobs:
  install-package:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Install Malicious Package
        run: npm install formio-tenant
